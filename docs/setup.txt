1. Tools : yeoman(scaffolding)
        bower : dependency management
        grunt : build system
        
command: npm install -g yo bower grunt-cli

2. Install angular generator.

command: npm install -g generator-angular

3. Scaffold an angular project, selecting coffescript over js, for productivity.
yo angular --coffee

4. Not usign sass + compass : no reason visible.

5. 